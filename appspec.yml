version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/deployment/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: deployment/deploy.sh
      timeout: 60
      runas: root
